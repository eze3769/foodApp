<%= render 'commons/navbar', shop:@shop%>
<%= render 'commons/sideMenu', shop:@shop %>

<div class="container">
   <ul class="orders_notes">
    <%=Order.find_by_shop(:Shop =>@shop).each do |order|%>
        <li>
            <a href="#">
            <h2>Order <%=order.id%></h2>
            <p> <%=order.product%></p>
            </a>
        </li>
    <%end%>
    </ul>
</div>